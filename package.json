{
  "name": "@trackflow/monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "TrackFlow monorepo — website, backoffice, business logic, and future AI agents.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "uis/*"
  ],
  "scripts": {
    "bootstrap": "npm run build --workspace @trackflow/business-logic",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace @trackflow/business-logic",
    "build": "npm run build --workspaces --if-present",
    "dev:website": "npm run dev --workspace @trackflow/website",
    "dev:backoffice": "npm run dev --workspace @trackflow/backoffice",
    "verify:freight-quote": "node .agents/skills/freight-quote-invariants/scripts/verify.mjs"
  }
}
